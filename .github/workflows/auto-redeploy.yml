name: Twice Daily Vercel Redeploy

on:
  schedule:
    - cron: '0 0 * * *'   # Midnight UTC
    - cron: '0 12 * * *'  # Noon UTC

jobs:
  redeploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: |
          curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}