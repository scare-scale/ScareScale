---
import { getCollection ***REMOVED*** from "astro:content";
import MainLayout from "../layouts/MainLayout.astro";
import MoviesGrid, { Sort ***REMOVED*** from "../components/grids/MoviesGrid.astro";

const movieEntries = await getCollection("movie");
---

<MainLayout>
  <section
    class="flex flex-col gap-y-10 md:gap-y-20 pt-10"
  >
      <div class="mx-auto max-w-2xl px-6 lg:max-w-7xl lg:px-8">
        <p
          class="mx-auto m-4 max-w-lg text-center text-2xl font-semibold text-balance text-white sm:text-4xl"
        >
        All time Scariest Movies
        </p>
        <MoviesGrid entries={movieEntries***REMOVED*** showRank={true***REMOVED*** sort={Sort.HighestToLowestScore***REMOVED***/>
      </div>
      <div class="mx-auto max-w-2xl px-6 lg:max-w-7xl lg:px-8">
        <p
          class="mx-auto m-4 max-w-lg text-center text-2xl font-semibold text-balance text-white sm:text-4xl"
        >
        Scariest Movies 2025
        </p>
        <MoviesGrid entries={movieEntries***REMOVED*** showRank={true***REMOVED*** sort={Sort.HighestToLowestScore***REMOVED***/>
      </div>
  </section>
</MainLayout>
