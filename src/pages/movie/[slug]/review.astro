---
import MainLayout from '../../../layouts/MainLayout.astro';
import AddReview from '../../../components/AddReview.jsx';

export const prerender = false;

const { slug } = Astro.params;

if (!slug) {
  return Astro.redirect('/');
}
---

<MainLayout>
  <AddReview client:load slug={slug}/>
</MainLayout>