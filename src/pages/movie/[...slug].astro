---
import { getCollection ***REMOVED*** from "astro:content";
import Prose from "../../components/Prose.astro";
import MainLayout from "../../layouts/MainLayout.astro";
import { calculateOverallRating, fearLevelText, round ***REMOVED*** from "../../utils/scoreUtils";
import { SEO ***REMOVED*** from "astro-seo";

export async function getStaticPaths() {
  const entries = await getCollection("movie");
  return entries.map(
    (entry) => ({
      params: { slug: entry.slug ***REMOVED***,
      props: { entry ***REMOVED***,
    ***REMOVED***)
  );
***REMOVED***

const { entry ***REMOVED*** = Astro.props;
const { Content ***REMOVED*** = await entry.render();

const posterUrl = `https://www.themoviedb.org/t/p/w300_and_h450_bestv2${entry.data.tmdbPosterId***REMOVED***`;

const scareScaleRating = calculateOverallRating(entry.data.categoryRatings);
const scareScaleText = fearLevelText(scareScaleRating);
const parsedDate = new Date(String(entry.data.releaseDate));
const formattedDate = parsedDate.toLocaleDateString("en-GB", {
    day: "numeric",
    month: "long",
    year: "numeric"
***REMOVED***);
---

<head>
  <SEO
    title={`Scare Scale - ${entry.data.name***REMOVED*** Review & Scare Ratings`***REMOVED***
    description={`${entry.data.name***REMOVED*** (${parsedDate.getFullYear()***REMOVED***) — Rated for Gore (${entry.data.categoryRatings.gore***REMOVED***), Creepy Moments (${entry.data.categoryRatings.creepy***REMOVED***), Jumpscares (${entry.data.categoryRatings.jumpscares***REMOVED***), Suspense (${entry.data.categoryRatings.suspense***REMOVED***), and Psychological Horror (${entry.data.categoryRatings.psychological***REMOVED***). Discover how it ranks on the Scare Scale!`***REMOVED***
    canonical={`https://scarescale.com/movie/${entry.data.tmdbId***REMOVED***`***REMOVED***
    openGraph={{
      basic: {
        type: "website",
        url: `https://scarescale.com/movie/${entry.data.tmdbId***REMOVED***`,
        title: `Scare Scale - ${entry.data.name***REMOVED*** Horror Movie Ratings`,
        image: posterUrl,
      ***REMOVED***,
      optional: {
        description: `${entry.data.name***REMOVED*** — ${entry.data.description***REMOVED***. Find out how scary it is based on Gore, Creepy moments, Jumpscares, Suspense, and Psychological horror.`,
      ***REMOVED***,
    ***REMOVED******REMOVED***
  />
</head>

<MainLayout>
  <div class="flex flex-col items-center">
    <section class="flex flex-col gap-y-10 pt-6" id="movieDetails">
      <nav aria-label="Breadcrumb">
        <ol
          role="list"
          class="mx-auto flex max-w-2xl items-center space-x-2 px-4 sm:px-6 lg:max-w-7xl lg:px-8"
        >
          <li>
            <div class="flex items-center">
              <a
                href="/"
                class="mr-2 text-sm font-medium text-white hover:text-gray-300"
                >Movie</a
              >
              <svg
                width="16"
                height="20"
                viewBox="0 0 16 20"
                fill="currentColor"
                aria-hidden="true"
                class="h-5 w-4 text-white"
              >
                <path d="M5.697 4.34L8.98 16.532h1.327L7.025 4.341H5.697z"
                ></path>
              </svg>
            </div>
          </li>
          <li class="text-sm font-medium text-white">
            {entry.data.name***REMOVED***
          </li>
        </ol>
      </nav>

      <div
        class="place-items-center max-w-2xl px-6 grid grid-cols-1 lg:max-w-7xl lg:grid-cols-3 lg:gap-x-8 lg:px-8"
      >
        <img
          src={posterUrl***REMOVED***
          alt={`${entry.data.name***REMOVED*** movie poster`***REMOVED***
          class="size-full size-2/3 rounded-lg object-cover"
        />

        <div
          class="py-10 lg:border-l lg:border-gray-200 lg:pt-6 lg:pl-8 lg:pb-16 lg:col-span-2"
        >
          <h1 class="text-2xl font-bold tracking-tight text-white sm:text-3xl">
            {entry.data.name***REMOVED***
          </h1>
          <h1 class="text-sm tracking-tight text-white">
            {`Released: ${formattedDate***REMOVED***`***REMOVED***
          </h1>
          <div class="mt-5">
            <Prose>
              <Content />
            </Prose>
          </div>

          <h5 class="text-xl mt-5 font-medium text-white">Ratings</h5>
          <div class="grid grid-cols-2 gap-4">
            <ul role="list" class="divide-y divide-gray-100">
              {
                entry.data.categoryRatings ? (
                  Object.entries(entry.data.categoryRatings).map(
                    ([categoryName, score]) => (
                      <li class="flex justify-between gap-x-6 py-5">
                        <div class="flex min-w-0 gap-x-4">
                          <img
                            class="w-6 h-6 flex-none"
                            src={`/icons/categories/${categoryName***REMOVED***.svg`***REMOVED***
                            alt=" "
                          />
                          <h1 class="text-sm/1 font-bold text-white">
                            {categoryName***REMOVED***
                          </h1>
                        </div>
                        <p class="text-sm/1 text-white">{score***REMOVED*** / 10</p>
                      </li>
                    )
                  )
                ) : (
                  <h5 class="text-l m-5 font-medium text-white">
                    Ratings not found
                  </h5>
                )
              ***REMOVED***
            </ul>

            <div
              class="w-full max-w-sm rounded-lg shadow-sm bg-white content-center outline-none ring-2 ring-black"
            >
              <div class="flex flex-col items-center p-4">
                <!-- <h5 class="text-xl mb-4 font-medium text-white">Scare Scale</h5> -->
                <img
                src="/logo/logo_full.png"
                alt="Scare Scale Logo"
                class="h-14 w-auto"
              />
                <div
                  class="w-20 h-20 m-6 rounded-full shadow-lg border-2 border-[#d60c16] flex justify-center items-center"
                >
                  <h5 class="text-xl font-medium text-[#d60c16]">
                    {round(scareScaleRating)***REMOVED*** / 10
                  </h5>
                </div>
                <h5 class="text-2xl text-black font-bold">{scareScaleText***REMOVED***</h5>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>
</MainLayout>
